What key changed need to be made to configure a docker contrainer to execute as a non-root user 

Key Enhancements:
 1. User Creation: Added groupadd and useradd to create a dedicated system user (appuser) with no login shell.
 2. Secure Ownership: Used the --chown=appuser:appuser flag in the COPY instructions to ensure the application source code and models are owned by the non-root user.
 3. Principle of Least Privilege: Added the USER appuser instruction before the EXPOSE and CMD commands, ensuring the application process runs with restricted permissions.


How to make Terraform files more secure and robust

While not strictly required for the app to run, you should consider updating the metadata_startup_script to further harden the deployment:

   1. Add Security Flags: Since the container is now non-root, you can prevent it from gaining new privileges (like via setuid binaries) by adding --security-opt=no-new-privileges to the
      docker run command.
   2. Environment Variables: Ensure any sensitive configuration is passed securely.

  Suggested change to terraform/main.tf:

  I recommend updating the docker run line in your metadata_startup_script to include hardening flags:

   1 # Inside google_compute_instance.app_server metadata_startup_script
   2 docker run -d \
   3   --name NAME \
   4   -p S_PORT:D_PORT \
   5   --restart unless-stopped \
   6   --security-opt=no-new-privileges \
   7   APP_IMAGE


Can't access the webapp at the http://IP:30080/api/health

The issue is likely that the Docker image is built only on the master node, but the Kubernetes deployment is trying
  to run the pods on the worker nodes. Since you have imagePullPolicy: Never in your deployment.yaml, the worker
  nodes are failing because they don't have the wildfire-app:latest image locally.

  Additionally, because your cluster is using containerd as the runtime (configured in k8s-setup.sh), images built
  with docker build must be imported into the containerd image store for Kubernetes to see them.

  Recommended Fixes

  1. Update deploy.sh to build and import images on all nodes
  Modify the "Build Docker Image" section to loop through all nodes (master and workers), build the image, and then
  import it into containerd.

    1 # ... after copying files to master ...
    2
    3 # Loop through all nodes to build and import the image
    4 for NODE in $ALL_NODES; do
    5     echo "--- Building and Importing Image on $NODE ---"
    6     # Copy files to the node (if not already there)
    7     if [ "$NODE" != "k8s-master" ]; then
    8         gcloud compute ssh k8s-master --zone=us-central1-a --command "tar czf - main.py requirements.txt
      Dockerfile fire-models/" | \
    9         gcloud compute ssh $NODE --zone=us-central1-a --command "tar xzf -"
   10     fi
   11     
   12     # Build and import
   13     gcloud compute ssh $NODE --zone=us-central1-a --command "
   14         sudo docker build -t wildfire-app . && \
   15         sudo docker save wildfire-app | sudo ctr -n k8s.io images import -
   16     "
   17 done
   18
   19 # Deploy Application
   20 # ...

  2. Verify Pod Status
  Check if the pods are actually running. Since they are in a specific namespace, you must include the -n flag:

   1 kubectl get pods -n wildfire-apps -o wide
  If you see ErrImageNeverPull or ImagePullBackOff, it confirms the workers are missing the image.

  3. Use the correct IP
  Ensure you are using the External IP of a worker node (not the internal one). You can find these by running:


Received Error:
